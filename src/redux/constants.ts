const REQUEST = 'REQUEST';
const SUCCESS = 'SUCCESS';
const FAILURE = 'FAILURE';
const RESET = 'RESET';

const LOG_IN = 'LOG_IN';
const CHECK_AUTH = 'CHECK_AUTH';

export const LOG_IN_REQUEST = `${LOG_IN}_${REQUEST}`;
export const LOG_IN_SUCCESS = `${LOG_IN}_${SUCCESS}`;
export const LOG_IN_FAILURE = `${LOG_IN}_${FAILURE}`;
export const LOG_IN_RESET = `${LOG_IN}_${RESET}`;

export const CHECK_AUTH_REQUEST = `${CHECK_AUTH}_${REQUEST}`;
export const CHECK_AUTH_SUCCESS = `${CHECK_AUTH}_${SUCCESS}`;
export const CHECK_AUTH_FAILURE = `${CHECK_AUTH}_${FAILURE}`;
export const CHECK_AUTH_RESET = `${CHECK_AUTH}_${RESET}`;
